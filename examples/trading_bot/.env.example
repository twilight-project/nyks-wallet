
# =============================================================================
# NYKS WALLET ENVIRONMENT CONFIGURATION
# =============================================================================
# This file contains all the environment variables needed to run the nyks-wallet application.
# Copy this file to .env and fill in the appropriate values for your environment.

# =============================================================================
# CORE CONFIGURATION
# =============================================================================
RUST_LOG=info,debug # Logging level for Rust applications (e.g., info, debug, trace)
RUST_BACKTRACE=full # Enable Rust backtrace for debugging
CHAIN_ID=nyks # The identifier for the blockchain network

# =============================================================================
# NETWORK AND RPC ENDPOINTS
# =============================================================================
# These are the URLs for various services the wallet interacts with.

# Local development endpoints, need to be changed for production environment
FAUCET_BASE_URL=http://0.0.0.0:6969 # Faucet for test tokens
NYKS_LCD_BASE_URL=http://0.0.0.0:1317 # Nyks chain Light Client Daemon (LCD) endpoint
NYKS_RPC_BASE_URL=http://0.0.0.0:26657 # Nyks chain RPC endpoint

# Order-wallet feature endpoints, required for order-wallet feature
RELAYER_API_RPC_SERVER_URL=http://0.0.0.0:8088/api # Relayer public API endpoint (required for order-wallet)
PUBLIC_API_RPC_SERVER_URL=http://0.0.0.0:8088/api # Public API endpoint (can be same as relayer, required for order-wallet)
RELAYER_RPC_SERVER_URL=http://0.0.0.0:3032 # Relayer client API endpoint (required for order-wallet)
ZKOS_SERVER_URL=http://0.0.0.0:3030 # zkos server endpoint for zero-knowledge operations (required for bin relayer-init also)

# =============================================================================
# WALLET CONFIGURATION
# =============================================================================
RELAYER_PROGRAM_JSON_PATH=./relayerprogram.json # Path to the relayer program JSON file
NYKS_WALLET_PASSPHRASE=test1_password # Passphrase for the Nyks wallet, Leave empty if you want to use passphrase prompt
WALLET_ID= # Optional: Specify a wallet ID to use


# =============================================================================
# DATABASE CONFIGURATION
# Required for the order-wallet feature, which is enabled by default.
# =============================================================================
DATABASE_URL_POSTGRESQL=postgresql://username:password@localhost:5432/database_name # Connection string for PostgreSQL
DATABASE_URL_SQLITE="./wallet_data.db" # Path to the SQLite database file


# Nyks Trading Bot Configuration

# =============================================================================
# TRADING CONFIGURATION
# =============================================================================
DEFAULT_GAS=200000 # Default gas limit for transactions
DEFAULT_FEE=1000 # Default transaction fee
TRADING_FEE_RATE=0.001 # Fee rate for trading operations (as a decimal)

# =============================================================================
# RISK MANAGEMENT
# =============================================================================
MAX_POSITION_SIZE=50000 # Maximum size of a trading position
MAX_DAILY_LOSS=10000 # Maximum allowed loss in a single day
EMERGENCY_STOP_THRESHOLD=0.1 # Threshold for emergency stop-loss (as a decimal)

# =============================================================================
# LOGGING
# =============================================================================
LOG_TO_FILE=true # Enable or disable logging to a file (true/false)
LOG_DIRECTORY=./logs # Directory to store log files

# =============================================================================
# DEVELOPMENT
# =============================================================================
PAPER_TRADING=true # Enable paper trading mode for simulated trading (true/false)
ENABLE_BACKTESTING=false # Enable backtesting mode (true/false)

# =============================================================================
# NOTES
# =============================================================================
# 1. Ensure that all paths are correct for your system configuration.
# 2. For production environments, use secure passwords and manage keys carefully.
# 3. The local development URLs assume all services are running on the local machine.
